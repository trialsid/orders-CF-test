{
  "name": "order-ieeja",
  "private": true,
  "type": "module",
  "scripts": {
    "build:inventory": "node scripts/generate-inventory.mjs",
    "predev": "npm run build:inventory",
    "dev": "vite",
    "prebuild": "npm run build:inventory",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",

    "pages:dev": "npm run build && wrangler pages dev dist",
    "pages:deploy": "npm run build && wrangler pages deploy dist",
    "pages:deploy:preview": "npm run build && wrangler pages deploy dist",
    "pages:deploy:prod": "npm run build && wrangler pages deploy dist --branch production",

    "db:migrate": "npx wrangler d1 migrations apply order_ieeja_orders --local",
    "db:migrate:remote": "npx wrangler d1 migrations apply order_ieeja_orders --remote",
    "db:migrate:status": "npx wrangler d1 migrations list order_ieeja_orders --local",
    "db:migrate:status:remote": "npx wrangler d1 migrations list order_ieeja_orders --remote",

    "db:pull": "npm run db:pull:export && npm run db:pull:process && npm run db:pull:import",
    "db:pull:force": "node -e \"require('fs').rmSync('./.wrangler/state', { recursive: true, force: true });\" && npm run db:pull",
    "db:pull:export": "npx wrangler@latest d1 export order_ieeja_orders --remote --output backup.sql",
    "db:pull:process": "node scripts/reorder-d1-export.js backup.sql",
    "db:pull:import": "npx wrangler@latest d1 execute order_ieeja_orders --local --file backup.sql",

    "db:query:users": "npx wrangler@latest d1 execute order_ieeja_orders --local --command \"SELECT phone, role, status FROM users;\"",
    "db:query:users:remote": "npx wrangler@latest d1 execute order_ieeja_orders --remote --command \"SELECT phone, role, status FROM users;\"",

    "db:wipe:orders": "npx wrangler d1 execute order_ieeja_orders --local --command \"DELETE FROM order_items; DELETE FROM orders;\"",
    "db:wipe:orders:remote": "npx wrangler d1 execute order_ieeja_orders --remote --command \"DELETE FROM order_items; DELETE FROM orders;\"",
    "db:wipe:users": "npx wrangler d1 execute order_ieeja_orders --local --command \"DELETE FROM users WHERE role != 'admin';\"",
    "db:wipe:users:remote": "npx wrangler d1 execute order_ieeja_orders --remote --command \"DELETE FROM users WHERE role != 'admin';\""
  },
  "dependencies": {
    "lucide-react": "^0.465.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.9.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "autoprefixer": "^10.4.20",
    "csv-parse": "^6.1.0",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "wrangler": "^4.45.3"
  }
}
